import styled from 'styled-components';

const User = () => {
	return <UserWrapper>User</UserWrapper>;
};
export default User;

const UserWrapper = styled.div`
	width: 405px;
	height: 120px;
	margin-bottom: 20px;
`;
